@page "/seeallbookings"
@using BPCProtoTypeBlazorV3.Client.ViewModel
<h1>See All Bookings</h1>
<h2>TEST TEST TEST</h2>

@foreach (var b in bookingView.Bookings)
{
    <div class="container">
        <div class="col-12">
            <label>@b</label>
            <label>@b.StartAddress</label>
            <label>@b.StartCity</label>
        </div>
    </div>
}

@code
{
    BookingViewModel bookingView = new BookingViewModel();

    protected override async Task OnInitializedAsync()
    {
        await bookingView.GetBookings();
    }
}

